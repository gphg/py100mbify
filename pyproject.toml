[project]
name = "py100mbify"
version = "0.1.0"
description = "A command-line tool for compressing video files to a target size."
requires-python = ">=3.8"
dependencies = []

[project.scripts]
py100mbify = "py100mbify.__main__:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
